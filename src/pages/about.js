import * as React from "react";
import Layout from "../components/Layout/Layout";

const AboutPage = () => {
    return (
        <Layout pageTitle="About CGM School">

        </Layout>
    );
};

export default AboutPage;